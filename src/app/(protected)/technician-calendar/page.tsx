"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function TechnicianCalendar(){const [calls,setCalls]=useState<any[]>([]),[date,setDate]=useState(new Date().toISOString().slice(0,10));useEffect(()=>{fetch(`/api/field-tech?date=${date}`).then(r=>r.json()).then(d=>setCalls(d.calls||[]))},[date]);return <div dir="rtl" className="space-y-5"><div className="flex justify-between"><div><h1 className="text-3xl font-black">לוח עבודה לטכנאים</h1><p style={{color:"var(--muted)"}}>יומן TECHNICIAN_SERVICE בלבד</p></div><Link className="btn-accent" href="/technicians">ניהול טכנאים</Link></div><div className="card p-4"><label>תאריך <input className="input-field inline-block mr-2" type="date" value={date} onChange={e=>setDate(e.target.value)}/></label></div><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-3">{calls.map(c=><div className="card p-4" key={c.id}><div className="font-bold">{c.technician?.name||"ללא שיבוץ"}</div><div>{c.customer?.name} · {c.callNumber}</div><div className="text-sm" style={{color:"var(--muted)"}}>{c.scheduledAt?new Date(c.scheduledAt).toLocaleTimeString("he-IL",{hour:"2-digit",minute:"2-digit"}):"ללא שעה"} · {c.status}</div></div>)}{!calls.length&&<div className="card p-8">אין קריאות ליום זה</div>}</div></div>}

