"use client";
import {useEffect,useState} from "react";import Link from "next/link";
export default function TechnicianMap(){const [rows,setRows]=useState<any[]>([]);useEffect(()=>{fetch("/api/technicians").then(r=>r.json()).then(d=>setRows(d.technicians||[]))},[]);return <div dir="rtl" className="space-y-5"><div className="flex justify-between"><div><h1 className="text-3xl font-black">מפת טכנאים</h1><p style={{color:"var(--muted)"}}>מיקום מוצג רק למשתמשים מורשים</p></div><Link className="btn-accent" href="/service-map">מפת לקוחות</Link></div><div className="card p-5"><p className="mb-4">שכבת המפה מוכנה לנתוני GPS. הטבלה מציגה את הטכנאים שיש להם מיקום בסיס או מיקום אחרון.</p><div className="grid md:grid-cols-2 lg:grid-cols-3 gap-3">{rows.map(t=><div className="border rounded-xl p-4" key={t.id}><div className="font-bold">{t.user.name}</div><div className="text-sm">{t.baseCity||"ללא עיר"} · {t.status}</div><div className="text-xs mt-2" style={{color:"var(--muted)"}}>{t.locations?.[0]?`${t.locations[0].lat}, ${t.locations[0].lng}`:"אין מיקום אחרון"}</div><Link className="btn-primary text-xs mt-3 inline-block" href={`/technicians/${t.id}`}>כרטיס טכנאי</Link></div>)}</div></div></div>}

