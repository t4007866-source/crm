# שי סחר CRM — שדרוג מרכז שירות והזמנות

השדרוג שומר על המודולים, הלקוחות, הלידים, ההרשאות, יומני לידים/שירות/החלפות סננים, PostgreSQL ו-Prisma.

## נוסף בגרסה זו
- עריכה מהירה להזמנות ושמירת תזמון.
- לוח שיבוץ רב־טכנאי עם Drag & Drop.
- בדיקות חפיפה והתרעת זמן נסיעה לפני שיבוץ.
- תור משימות ללא שיבוץ והפרדה להתקנות, שירות והחלפות סננים.
- חלונית עריכה מהירה לקריאה עם סטטוס, רכב, חלפים והערות.
- קישורי ניווט, חיוג ו-WhatsApp.
- רשימת קריאות פתוחות עם קישור למפת השירות.
- API להחלפות סננים תקופתיות.
- מודלים לרכב טכנאי ומלאי לכל רכב.
- תמיכה בשמירת חלפים/בדיקות וחתימת לקוח בקריאת שירות דרך API.

## הפעלה לאחר העלאה

```bash
npx prisma generate
npx prisma db push
npm run build
```

הפעלת `db push` נדרשת לאחר שינוי הסכמה כדי ליצור את טבלאות הרכב והמלאי לרכב.

