# שדרוג AI — שי סחר

השדרוג מוסיף שכבת AI נפרדת בלי לשנות את המודולים הקיימים:

- `/ai` — עוזר שאלות ותשובות על נתוני CRM עם מקור וטווח זמן.
- `/api/ai/assistant` — פעולות קריאה בלבד, עם סינון לפי הרשאת המשתמש.
- `/api/ai/customer-summary/[id]` — סיכום לקוח עם הזמנות, שירות ומערכות.
- `/api/ai/email-preview` — חילוץ אימייל לתצוגה מקדימה; אין כתיבה למסד.
- `/api/search` — חיפוש מאוחד בלקוחות, לידים, הזמנות, שירות וטכנאים.

## אבטחה

- העוזר אינו מקבל הרשאות גבוהות יותר מהמשתמש.
- נציג מכירות מוגבל לרשומות שהוקצו לו.
- נתונים פיננסיים זמינים רק ל-ADMIN/MANAGER.
- שאילתות AI וסיכומי לקוח נרשמים ב-AuditLog.
- פעולות כתיבה/שליחת הודעות אינן מופעלות בשלב זה.

## הפעלה וכיבוי

הוסף ב-Vercel וב`.env`:

```env
AI_ASSISTANT_ENABLED="true"
```

כדי להשבית את המודול בלי לפגוע בשאר המערכת:

```env
AI_ASSISTANT_ENABLED="false"
```

## פריסה

לפני העלאה:

```bash
npx prisma validate
npx prisma generate
npm run build
```

